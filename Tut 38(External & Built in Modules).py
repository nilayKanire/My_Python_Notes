import random      # we can see this module code written in backend by using keys ( cltr + Mouse click on name)

# Search Random module python in google and see the modules for the latest python version. Then we can see the
# 'function' in modules.


random_number = random.randint(0, 5)                     # If our module color is grey then it shows that we dont used the module but if it has color then it means we are using that module
print(random_number)
#
# #  ^^^
# #  ||| ___________This function will give us a random number from 0 to 5
#
#
rand = random.random() * 100         # here module k andar random function hai matlab 'Module k andar sub Module'
print(rand)
#
#
#
# list = ["Star Plus", "DD1", "Aaj Tak", "CodeWithHarry"]
# choice = random.choice(list)
# print(choice)



# range = list(range(10))
# print(range)

range = list(range(0, 10 , 3))
print(range)


